{
  "name": "apelix-business-manager-mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "npx @capacitor/cli sync",
    "open-android": "npx cap open android",
    "build-android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}